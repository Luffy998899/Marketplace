/** Flat platform commission on every transaction (30% = 3000 basis points). */
export const PLATFORM_TAKE_RATE_BPS = 3000;

/** Minimum wallet top-up in USD minor units (cents). */
export const MIN_TOP_UP_MINOR = 500; // $5.00

/** Maximum single wallet top-up in USD minor units (cents). */
export const MAX_TOP_UP_MINOR = 500_000; // $5,000.00

/** Default checkout currency for Phase 1 MVP. */
export const DEFAULT_CURRENCY = 'USD';
